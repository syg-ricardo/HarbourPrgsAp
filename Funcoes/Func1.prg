    /*
        Exemplo de funcoes com parametros
    */
    procedure main
        ? int( 12.456 ) //Retorna 12
        ? Year( Date()) //Retorna o ano da data que foi passada, no caso Date()
    return