PROCEDURE Main
    ? "OLA PESSOAL" // UTIL PARA EXPLICAR A LINHA CORRENTE
    // ? "ESSA LINHA TODA SERA IGNORADA"
    *  ? "ESSA LINHA TAMBÉM SERA IGNODARA"
    && ? "E ESSAS TAMBÉM"
    ? "O VALOR: ",1200 //UTIL PARA COMENTAR
                       //APOS A LINHA
    /*
    NOTE QUE ESTE COMENTARIO
    POSSUI VARIAS LINHAS
    */
RETURN 